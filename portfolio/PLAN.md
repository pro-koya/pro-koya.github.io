# Claude Code 実装プロンプト

以下の要件で、ポートフォリオサイトの「仕事観」セクション用に、**SVG/画像から点群を抽出して粒子アニメーション化するスクロール連動演出**を実装してください。

## 目的
- 4つのシーン画像（またはSVG）を元に、粒子が「集まる → 散る → 再び集まる」ことで場面が切り替わる演出を作る
- スクロール進行に応じて Scene1 → Scene2 → Scene3 → Scene4 へ遷移させる
- 左側にテキスト、右側に粒子アニメーションを表示する構成にする
- 実装は **Next.js / React** 前提
- アニメーション制御は **GSAP + ScrollTrigger**
- 描画は **Canvas ベース**
- SVGは直接アニメーションせず、**オフスクリーンCanvasに描画して点群抽出用マスクとして使う**

## 入力ファイル
以下の4ファイルを使用する想定です。

- `scene1_plow.svg`
- `scene2_plant.svg`
- `scene3_grow.svg`
- `scene4_deliver.svg`

SVGで扱いづらい場合は、PNG/JPGにも対応できる実装にしてください。

## 実装方針
1. 各SVG/画像をオフスクリーンCanvasに描画する
2. アルファ値または明度を基準に、一定間隔で点群を抽出する
3. 抽出した点群を各シーンのターゲット座標として保持する
4. メインCanvas上で粒子を描画する
5. 粒子は以下の流れで動く
   - Scene開始時：粒子がターゲット形状に集まる
   - Scene遷移時：粒子が一度散る
   - 次シーン：新しいターゲット形状へ再集合する
6. Scene3では、他シーンより少し粒子数を増やす、または拡張的に見える挙動を入れる
7. ScrollTriggerでスクロール進行率を管理し、4シーンを順に遷移させる
8. Stickyレイアウトで、左側テキスト・右側Canvasの構成にする

## 最低限必要な機能
- SVG/画像 → 点群抽出ユーティリティ
- 粒子クラスまたは粒子管理ロジック
- sceneごとのtargetPoints管理
- explode（散る）処理
- morph/reform（再集合）処理
- GSAP ScrollTrigger連携
- リサイズ対応
- パフォーマンスを考慮した実装

## 実装条件
- Canvasのみで描画する
- DOM上で大量SVG pathを直接操作しない
- requestAnimationFrameでレンダリングする
- 型はTypeScriptで書く
- ファイル構成は読みやすく分ける
- 過剰なライブラリ追加は避ける

## 希望する成果物
- 動作する React コンポーネント
- 点群抽出ユーティリティ
- 粒子アニメーションロジック
- GSAP ScrollTrigger 統合
- 必要な補助関数
- 実装メモ（どこを調整すれば粒子数・速度・拡散量を変えられるか）

## 補足
- 見た目の完全再現よりも、まずは「気持ちよく粒子が集まり、散り、次のシーンへ移る」ことを優先してください
- まずは動く最小構成を作り、その後に調整しやすい構造にしてください